p List of videos:
table
  tbody
    each item in posts
      tr
        td
          img(src=item.full_picture,width=100,height=100)
        td(width=500)
          a(href=item.link) #{item.name}


if paging
  p 
    button(onclick='getPosts(\'#{paging.previous}\')',disabled=!paging.previous) Previous

    button(onclick='getPosts(\'#{paging.next}\')',disabled=!paging.next) Next
        
  
  
